declare module "darkreader:inject-src" {
    // The bundled Dark Reader injectable source, produced by the
    // `darkreader-inline` esbuild plugin in esbuild.config.mjs.
    const darkReaderInjectSource: string;
    export default darkReaderInjectSource;
}
